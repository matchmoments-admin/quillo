<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Platform theme (no AppCompat/Material-Components dependency needed; the UI is Compose). -->
    <style name="Theme.TaxAgent" parent="android:Theme.DeviceDefault.Light.NoActionBar" />
</resources>
